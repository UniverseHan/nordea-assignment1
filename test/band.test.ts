import Band from '../src/band';
import { parseBand } from '../src/transform';

test('band', () => {
  expect(true);
});

// unique => sort and all i != i + 1 
// mention band.plays includes plays only in the bands objects
// mention: why not use enum or use enum. assume limited list of instruments.
// 
// 